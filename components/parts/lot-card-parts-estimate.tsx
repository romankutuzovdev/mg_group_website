import type { AuctionLot } from "@/lib/auctions/types";

/** Bamper API not available on static site — keep card layout intact. */
export function LotCardPartsEstimate(_props: { lot: AuctionLot }) {
  return null;
}
